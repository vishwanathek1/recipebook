"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
exports.__esModule = true;
exports.noopLog = exports.defaultLog = void 0;
var chalk_1 = __importDefault(require("chalk"));
var buffer_1 = require("./buffer");
exports.defaultLog = {
    start: function (socket) {
        socket.queryStartTime = process.hrtime();
    },
    finish: function (socket, task) {
        var time = (process.hrtime(socket.queryStartTime)[1] / 1000000).toFixed(1);
        if (task.failed)
            console.log(chalk_1["default"].bold.magenta("(" + time + "ms)") + ' ' + chalk_1["default"].bold.red(task.query));
        else
            console.log(chalk_1["default"].bold.cyanBright("(" + time + "ms)") +
                ' ' +
                chalk_1["default"].bold.blue(task.query));
        if (task.notices)
            task.notices.forEach(function (notice) {
                console.log(chalk_1["default"].bold.yellow("NOTICE:") +
                    ' ' +
                    chalk_1["default"].bold.whiteBright(notice.message));
            });
    }
};
exports.noopLog = {
    start: buffer_1.noop,
    finish: buffer_1.noop
};
