import { AdapterBase } from './adapterBase';
import { Prepared } from '../types';
export declare const prepare: <Args extends any[] = any[]>(adapter: AdapterBase, name: string, args: string[], query: string) => Prepared<Args>;
